# Keeps this directory in a fresh clone. Contents are runtime-generated by the
# weewx weather34 extension (w34realtime.txt, weewxserverinfo.txt) and gitignored.
# Deploy perms: weewx:www-data, mode 2775 (setgid) so weewx can write and PHP can read.
